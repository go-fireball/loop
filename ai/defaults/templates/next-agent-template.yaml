next_role: <ROLE>
handoff_notes: |
  <short baton context>
return_to: <ROLE>               # optional; only when next_role is HUMAN
escalated_by: <ROLE>            # optional
escalation_reason: <one line>   # optional
